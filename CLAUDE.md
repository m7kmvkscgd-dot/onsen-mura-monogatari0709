# 温泉村物語

`C:\ダンジョン1\` を複製して作った新プロジェクト。ゲームの仕組み(パーティ制・パーマデス・戦闘システム等)はダンジョン1から引き継ぎつつ、**和製(和風)の方向性に作り変えていく**ことが目的。ダンジョン1自体は元のまま残っており、このプロジェクトはそこから分岐した独立した作業ブランチ。

詳細な過去の作業履歴は docs/開発履歴.md を参照。

## 作業環境の前提(重要)
ユーザーは基本的にパソコンの前におらず、**スマホから遠隔で(リモートコントロールで)作業している**。Claude Codeとのやり取り自体もスマホ経由が基本という前提で動くこと。これを踏まえて:
- 何か確認を求める・操作させる時は「PCの前でこうしてください」ではなく、スマホでそのまま完結する方法を優先する(例: ローカルサーバーを自分で起動して確認、ではなく本番URL`https://onsen-mura-monogatari.pages.dev`をスマホのSafariで開いて確認、が基本ルート)
- 作業用ツール(スキルエディタ・世界地図エディタ等の`*.html`)を新規に作る時も、スマホのブラウザで開いて操作できることを前提にレイアウト・操作方法(タップ/ドラッグ)を設計する
- ローカルサーバー起動やPC上でのファイル確認など「PC前提」の手順が必要な場合は、その旨を明示し、代替のスマホ完結ルートが無いか先に検討する

## CLAUDE.md運用ルール
- このファイルは軽量に保つ。個別の作業ログ・コミット記録は docs/開発履歴.md の末尾に追記し、CLAUDE.mdには書かない。
- CLAUDE.md が 30,000バイトを超えたら古い記録を docs/開発履歴.md へ移す。

## せりふくんシステム
`serifu/`ディレクトリに「せりふくん(軽度ストレス)」というセリフ制作補助システムがある。詳細ルールは`serifu/CLAUDE_serifu.md`参照。
**ユーザーが「せりふくん軽度ストレス生成」のようにこのシステムでのセリフ生成を求めるコメントをしたら、まず現在起動しているモデルを確認する。Fable 5ならそのまま`serifu/CLAUDE_serifu.md`のルールに従って生成に入ってよい。Fable 5でなければ生成せず、「現在Fable 5になっていません」等と伝えて指示を待つこと**(このシステムは品質最優先でFable 5前提のため、無断でSonnet 5等にフォールバックしない)。

## モデル別の作業ガイド(2026-07-30ユーザー合意→2026-07-31改訂)
**【2026-07-31改訂】ユーザーがMax 20xへアップグレードしたため、基本方針は「全作業Fable 5」になった。** 以下の分担表は削除せず、**枠が逼迫した週(締め日前に残量が心細い時)のフォールバック手順**として残す。フォールバック運用時は従来どおり: **セッション開始時に自分がどのモデルか確認し、Fable 5でないのに下記「Fable 5専用」の作業を頼まれたら、着手前に「これはFable 5向きの作業です」と伝えて指示を待つこと**(せりふくんのFable 5限定ルールと同じ運用。ユーザーが「Sonnetでいいからやれ」と明示したら従ってよい)。

**Sonnet等でよい作業(軽い・構造的にバグりにくい)**:
- データテーブルの反映(エディタのエクスポートをdata.jsへ差し替え、数値調整)
- 素材の差し替え(SE/BGM/画像。バックアップ→コピー→デプロイの定型)
- 文言の変更、独立した新規ツールページ(*.html)の作成、既存パターンの複製(隣と同じものをもう1個)
- コミット・デプロイ・記録などの運用作業
- スキル追加のうち「同じ仕組みのスキルが既に存在するもの」(既存パッシブ/actionの部品にdata.jsから値を渡すだけのデータ入力。必ず既存スキルをコピーして改変し、node vmで発動テストを書くこと。フィールド名を間違えてもエラーにならず「何もしないスキル」が静かに完成する罠と、単一スロットのパッシブ枠が2個目で上書きされる罠に注意)

**Fable 5専用の作業(実際に事故った実績のある重い領域)**:
- `engine.js / battle.js / ui.js / effects.js / audio.js`を触る作業全般(戦闘描画層の差分レンダラー・戦闘計算・パッシブのフック・音声パイプライン)
- iOS固有の見た目/音の問題、fixed配置やスクロール連動などのレイアウト問題
- 既存コードの削除・改名・リファクタ(全使用箇所の追跡が必要。削除ミスで起動不能になった前科あり)
- タイミング・非同期・レース系(ウェーブ制御、演出待ち、ゴーストクリック等)
- 「動かない」系の原因調査(根本原因の特定が必要なデバッグ)、仕様設計の相談
- スキル追加のうち「前例のない効果」(engine.js/battle.jsに新しい仕組みが必要なもの。水月の強制ターゲット・覚悟・式神などはこちら側だった)。スキルの効果や数値の設計判断そのもの(上位互換禁止ルール等の帯域感覚が必要)もFable
- Sonnetで作業する場合の追加ルール: 既存コードの削除・改名は禁止(追加のみ)、1タスク1コミット、node --checkとデプロイ後diff検証を必ず行う

## バージョン履歴
- **温泉村物語v1.1**(gitタグ): 8職業和風化、温泉村の舞台美術(町/宿屋/深淵の森の昼夜背景)、瀕死+救出システム、敵妖怪40種フル刷新、難易度再調整(階層スケーリング強化)、温泉(ストレス回復施設)、戦闘UIの視認性改善(表示位置の完全固定化・敵タップ選択・ダメージポップアップ・ログ簡潔化)までを含む。デスクトップに`温泉村物語v1.1を起動.lnk`のショートカットあり
- **温泉村物語v2.3**(gitタグ): v1.1以降の一連のUI安定化(モバイル実機でのfixed配置修正、町画面の2x2ボタンレイアウト、スクロール引っかかり修正)と、ストレス表現の落書きオーバーレイ化+段階デバフ+発狂(行動不能)、宿屋の宿泊制への再編(HP/MP自動回復廃止)、パーティ編成を出発時選択に変更、名簿上限10人までを含む。デスクトップに`温泉村物語v2.3を起動.lnk`のショートカットあり(v1.1のショートカットも残置)

## 戦闘演出・透過立ち絵の刷新(2026-08-01、詳細はdocs/開発履歴.md同日の項)
- **戦闘アニメ総刷新(全て本編適用済み)**: 敵通常=予備動作つき踏み込み(着弾310ms同期)/敵大技=溜め(赤オーラ、暗転なし)→解放+赤ビネット(白フラッシュなし)/味方通常=ヒットストップ/必殺技=金色発光+技名帯+職業別SE8種(60%音量)→強踏み込み。**技名演出は対象選択後**(treeSkillFxAfterPick)。かばうのみ演出なし
- **大技予告=構え状態(2026-08-02、💢廃止)**: 予告の瞬間から発動の攻撃演出完了まで、敵カードに赤オーラ脈動+立ち絵の微振動(WAAPI。CSSアニメだと被弾スカッシュとanimation取り合いで止まる罠)+技名の紫短冊が出続ける。effects.jsのsyncBigAttackStance(updateEnemyCardから毎描画呼ぶデータ駆動、bigAttackPending/__stanceHold)。敵短冊はカード子要素で敵ごとに持つ(シングルトン廃止=複数敵同時予告・味方技名との取り合い解消)
- **KO演出**: 倒れSE(1.3秒遅延)+発狂顔切替→膝折れ1.5秒→カード畳み消滅→0.7秒→控え走り込み。療養テキスト表示あり
- **透過立ち絵は本採用**: 序盤13体+提灯童のENEMIES定義へ焼き込み(frameless+個体spriteScale)。旧TEST_TRANSPARENTテーブル廃止。雑魚サイズは木霊(101px)基準の遠近則、飛行はairborne浮遊+地面影。**納品PNGは下端の半透明ゴミをチェックしてから取り込む**(化け狸で足浮き事故)
- **シームレス戦闘遷移も本採用**(襲撃戦のみ従来の暗転)
- **iOS音量問題の標準対処**: audio.volumeはiOSで無効のため、python(miniaudio+lameenc)で音源に焼き込む(タイトルBGM55%・洞窟環境音40%で解消済み。元音源はgit履歴)
- 残り実機確認: 新モーション/シームレス/KO演出の通しプレイ

## 次回このプロジェクトを再開する時の手順
1. `C:\温泉村物語\` を開く(このCLAUDE.mdが自動的に読み込まれる)。デスクトップの`温泉村物語 Claude Codeを起動.lnk`をダブルクリックすると、このディレクトリで`claude`が起動した状態のコマンドプロンプトがそのまま開く(コード上は`cmd.exe /k "cd /d C:\温泉村物語 && claude"`)。スマホからも指示を送りたい場合は`温泉村物語0711.lnk`(または既存の`温泉村物語 Claude Code(リモート)を起動.lnk`、中身は同じ)を使う。こちらは`claude --remote-control`付きで起動し、リモート操作(スマホ等)を有効にする
2. ローカルサーバーが必要な場合は `python nocache_server.py`(ポート8127)を`C:\温泉村物語\`で起動、またはデスクトップの`温泉村物語v2.3を起動.lnk`をダブルクリックすると`index.html`を直接ブラウザで開ける(file://起動、サーバー不要。ただしlocalStorageのセーブは同じ起動経路を使い続けないと引き継がれない点に注意)
3. スマホ実機で確認したい場合はCloudflareトンネルを新規に立てる必要がある(トンネルは使い捨てで、前回のURLは失効している)。「スマホで見たい」と伝えれば都度セットアップする。**あるいは本番URL`https://onsen-mura-monogatari.pages.dev`(下記「Cloudflare Pages本番デプロイ」参照)をそのままスマホのSafariで開いてもよい**(常時稼働・トンネル不要)
4. 作業を続ける際は本ファイル(CLAUDE.md)の「バージョン履歴」と各見出しの実装メモを読めば経緯を追える。gitログ(`git log --oneline`)も各コミットが1機能単位でまとまっている

## Cloudflare Pages本番デプロイ(重要な運用ルール)
このプロジェクトは`https://onsen-mura-monogatari.pages.dev`にCloudflare Pagesとして常時公開されており、ユーザーは主にこのURLを実機Safariで開いて動作確認する(トンネルより優先度が高い確認手段)。デプロイは以下のコマンドで行う:
```
npx wrangler pages deploy . --project-name=onsen-mura-monogatari --branch=main --commit-dirty=true
```
**`--branch=main`を必ず明示すること。** このオプションを省略すると、wranglerはgitの現在のローカルブランチ名(このリポジトリでは`master`)を使ってデプロイしてしまい、Cloudflare Pages側のProduction環境は`main`ブランチにひも付いているため、`--branch`未指定だとPreview環境にしか反映されず本番URLが更新されない(実際にこれで「直したのにスマホでは直っていない」という事故が一度発生した)。
デプロイ後は必ず`curl -sL https://onsen-mura-monogatari.pages.dev/index.html`等でファイル内容を取得し、ローカルの該当ファイルと`diff`で完全一致することを確認してから完了報告すること(SPAフォールバックにより存在しないパスでも200が返るため、ステータスコードだけでは正しくデプロイされたか判別できない)。本番エイリアスURL(`onsen-mura-monogatari.pages.dev`)は個別デプロイURL(`https://<hash>.onsen-mura-monogatari.pages.dev`)よりCDNキャッシュの反映が数秒〜数十秒遅れることがあるため、diffが一致しない場合は15〜20秒待って再確認する。

## 要望・バグ報告リスト(2026-07-29新設、重要な運用ルール)
ユーザーはClaude Codeを起動していない時に見つけたバグ/要望を`https://onsen-mura-monogatari.pages.dev/requests.html`(tools.htmlからもリンク)に書き込む(スクショ添付可)。保存先はCloudflare KV(REQUESTS_KV、wrangler.tomlでバインド)、API実装はfunctions/api/。
**ユーザーが「要望修正して」「要望リスト見て」等と言ったら**:
1. `curl -sL https://onsen-mura-monogatari.pages.dev/api/requests`で一覧取得(`status:"open"`が未対応。画像付き項目は`/api/requests/<id>`で画像込みJSONを取得し、base64をデコードした画像ファイルをReadで見る)
2. 対応が終わった項目は`curl -X PATCH .../api/requests/<id> -H "Content-Type: application/json" -d '{"status":"done","note":"..."}'`で対応済みにする。**noteはユーザーがページで読む報告文**なので、日本語で「何をどう直したか+デプロイ済みか」を書く(PATCHボディはUTF-8ファイルに書いて`--data-binary @file`で送るとシェルの文字化けを避けられる)
3. 対応しない/できない/仕様どおり、と判断した項目も黙って放置せず、理由をnoteに書いてdoneにする
ユーザーがページ側で「確認OK」を押すとclosedになり一覧から消える。テスト項目等の物理削除は`npx wrangler kv key delete --namespace-id=ea3db5084ac34bbcb359c988ff4729cb "req:<id>" --remote`。

## ダンジョン1から引き継いだ仕組み(ゲームエンジン部分は共通)
- **3人パーティ+控え1人**制、ターン制バトル(2026-07-26の大転換で4人制から変更。出発時に4人選び、4人目=控え。戦闘中は「交代」コマンド=ターン非消費で出たキャラが即行動・パーティ共有CD3ターン。倒れたら控えが無料で自動登場しCDが3にリセット。経験値は参加ターン比=出場ターン数÷戦闘総ターン数で按分)
- MP制(魔法系だけでなく物理必殺技もMP消費。町でのみ回復)
- 疲労度システム(潜り続けると弱体化、町で回復)
- **パーマデスは2026-08-01に標準廃止**: 戦闘不能=「重傷」(status:"injured")となり遠征から強制離脱、本人ストレス+100、温泉村で2日間(INJURY_REST_DAYS)の湯治後に自動復帰(HP全快・ストレスは残る。tickInjuryRecovery)。全員療養中はゲームオーバーにならない。設定の「パーマデスモード」トグルON時のみ従来の即ロスト+名簿削除。英霊システム構想は廃案(旧・瀕死+担ぐ+救出システムは2026-07-26の3人化転換で全廃済み。昼夜は完全に見た目のみ。旅団旗の建物効果(5人編成解禁)は無効化済みで再設計待ち)
- 複数敵・範囲攻撃(1〜3体の集団が出現し、範囲技持ちが雑魚の群れに有効)
- 職業ごとの武器/防具(3段階、レベルで解禁、購入すると同職業全員に永久ステータス加算)
- ダンジョン探索は線形進行(グリッド移動なし)。ただしスレスパ風の進路選択あり: フロア前進時に1〜2択の道(林道/獣道/暗い道/静かな道/木漏れ日/光る道/不穏な道/神隠しの道、海岸ステージは別テーマ)から選び、道ごとに戦闘率/財宝率/奇襲/金倍率が変わる。おみくじの結果も抽選に影響(dungeon.jsのPATH_DEFS参照)
- スマホ前提のレイアウト

## 完成に向けた優先順位(2026-07-26更新)
0-3. **ボステストモード+ボスギミック機構+ギミックエディター: 実装済み(2026-07-31)、実機確認まだ**: 3点セット(①gimmicks.js=トリガー(hpBelow/battleStart/round)→効果(overlay場演出/bg背景差替/fieldDamage周期場ダメージ/summon周期召喚)の汎用機構 ②タイトルの「ボステスト」=大規模戦テスト置き換え。味方4枠の職業/Lv/型/装備一括トグル+敵ボス1+お供3枠+ステータス上書き+ENEMIES_CHANGED形式JSON出力+テストモード中のみ戦闘画面に「タイトルへ戻る」 ③敵エディターのギミック欄=ボス限定の自由記述メモgimmickNotes、エクスポート同梱)。試作ボスboss_kasha(業火纏い)/boss_gashadokuro(骨呼び+骸骨召喚)はquestOnly+maxFloor:0でテスト専用隔離。運用ループ: ユーザーがエディターにギミックを書く→Fable5がgimmicks構造化データへ翻訳実装→ボステストで検証。node vm40+108テスト通過。**残り: 実機確認、火車の炎上背景など専用背景画像(ユーザーのChatGPT作、来たらoverlay→bg効果へ差し替え)、試作ボスの本番マップ配置**。詳細は`docs/開発履歴.md`2026-07-31の項と`docs/引き継ぎ_ボステストとギミックエディター.md`
0. **戦闘UIの差分更新化: 全フェーズ完了(2026-07-26)**: カードDOM使い回しの差分更新へ移行済み(敵カード列+味方バー3画面共通)、フェーズ4実機確認もユーザーが実施し問題なし、フェーズ5絆創膏撤去も完了(踏み込み復帰機構の撤去・揺れの完走化・ポップ負delayは画面跨ぎ用に存続判定)。経緯と実装状況は`docs/引き継ぎ_戦闘UI差分更新化.md`冒頭の追記を参照。**残り**: フェーズ5撤去分(踏み込み・揺れ・ポップの見た目)の実機確認だけユーザーがまだ。今後この描画層を触る時は「一回きりCSSアニメはクラスの無→有遷移でだけ再生/再発火は除去→リフロー→付与」の原則を守ること
0-1. **【積み残し】敵の大技予告に狙われている味方のターゲットマーク+致死警告(2026-07-25、コミット44be95f)、実機確認まだ**: 大技予告(bigAttackPending)が立った瞬間に単体大技の対象を確定し(engine.jsのcommitBigAttackTelegraphTarget、以前は発動時まで対象未定だった仕様を変更)、狙われた味方カードに赤い照準マーク(CSS/SVGのみ、画像素材なし)を表示。タップで敵名/技名の説明、未かばう想定でHPが最大20%以下まで落ち込む可能性が高い場合は左上に⚠️も追加表示しこちらもタップで説明。node vmでの検証は済みだが実機での見た目確認はまだ。**注意: この予告対象確定が「かばうが大技に効かない」バグの原因になっていたのを2026-07-26に修正済み(e45a892)。発動時のかばう/挑発が予告対象より優先される**
0-2. **【次回の再開ポイント(2026-07-28時点)】**: ①**襲撃システム本体は実装済み**(スケジュール進行・明朝発生・カウントダウンUI・重み付き抽選・敗北=村レベル-1(施設は残る)・日次収入=村Lv×5G/日・バリケード建築(木の柵/鉄杭柵・耐久永続・比例修理)・遠征中は帰還後の朝に延期。raid.js+data.jsのRAID_CONFIG参照)。**ウェーブ設計は2026-07-29にユーザーが襲撃エディタで作成したものを反映済み**(村Lv1〜4に個別設定、firstRaidDay7・repeatMin6〜Max8。村Lv5〜7はLv4プールへフォールバック、raid.jsのpickRaidWave仕様どおり)。node simulationで全レベルの抽選フォールバックを確認済み。**残り: 実機確認まだ**(村Lv5〜7分の追加ウェーブや、フォールバックのままで良いかはユーザー判断待ち)。**同日、タイトル画面に「襲撃テスト(サイコロ)」も新設**: 平均レベルだけ指定すると防衛隊人数/職業/スキル/見張り台・バリケード・投石器の有無/村レベルを全部ランダムに振って実際の襲撃開始処理(startRaidBattleFromPrep)を呼ぶ。node vm5000回試行で不変条件確認済み、実機確認はまだ ②**採掘場は実装済み(2026-07-28)**: 巨木=森12層(木を切る最大4回・スト+15/回・専用背景5枚)、鉄鉱石=洞窟6層(最大3回・スト+20/回・専用背景は未提供)。作業者1人固定・スト100超え採取不可・帰り道でも残数消化可・残数リセットは村帰還ごと(=1遠征で木4/鉄3、ユーザー確定) ③鍛冶屋フェーズ2(装備2択+打ち直し)も承認済み将来案 ④**開発ツール統合進行中(2026-07-29)**: 入口=`tools.html`(全ツールのダッシュボード)。方針は「入口は1つ・データは1か所」でSPA化はしない。襲撃エディタ+奉行所エディタ(2026-07-30)は内蔵データコピーを撤廃しdata.js直読み化済み(「直結」バッジ)。残り: enemy/skill/building/world_mapエディタも同じ手術で順次直結化(直結化したエディタは「エディタ内蔵ベースラインの手動同期」ルール自体が不要になる) ⑥**クエストダンジョン(奉行所限定ルート、モンハン形式)ゲーム本体まで実装済み(2026-07-30)、実機確認まだ**: 序盤の景色の飽き対策の専用ルート。最終仕様(同日ユーザーが2度改訂して確定。「期限切れターゲット消失」案・「受注=即出発」案はどちらも破棄): ①**受注はいつでも可**(契約金前払い)。受注後も村・通常探索(森出発ボタン)は自由 ②依頼ごとに**出発可能時間**(QUEST_DEFSの`departStart`/`departEnd`、"11:00"形式文字列、未設定=いつでも)があり、受注中は編成画面に専用出発ボタン(departQuestRouteBtn)が出る。時間外に押すと「◯時まで時間を進めて出発しますか？」確認(今日の枠を過ぎていたら「明日の◯時」表記で翌日へ)から時計を送って出発 ③**期限なし・一発勝負**: 討伐成功=即時報酬+契約金返還(既存victory処理をそのまま流用)、撤退/全滅=失敗(リザルトに赤の失敗カード)・契約金没収・当日再受注不可・**翌日クールダウン無視で必ず再張り出し** ④出発前の取りやめ(abandonQuest)だけは契約金返還 ⑤ルート=`currentStage:"questroute"`の一本道(進路選択・茶屋・分かれ道・採掘なし、totalFloorsで行き止まり)。背景と敵はQUEST_ROUTE_DEFSのsegments(fromFloor/bg/enemies)から。敵指定が空の区間は森の通常抽選を流用、targetFloorで確定戦闘(逃げたらchasing追跡も従来どおり)。実装箇所: dungeon.js(questRouteDef/questRouteSegmentFor/pickEncounterForQuestRoute/advanceBtn分岐/finishRetreat失敗精算/遠征スナップショットにquestRouteId)、town.js(奉行所カードの出発時間表示・acceptQuest・出発ボタン+時計送り)、battle.js(全滅時の失敗精算)、ui.js(ルート背景・リザルト失敗カード)、state.magistrateQuestFailedOn新設、奉行所エディタにroute/departStart/departEnd入力欄。node vm30テスト全通過(受注/返金/失敗ゲート/再張り出し保証/時計送り/区間解決/抽選)。**2026-07-31に物語クエスト量産パイプラインへ発展→同日夜に縮小方針へ転換(重要)**: GPT/CodexがクエストをJSON(schemaVersion 1)で量産しClaudeが取り込む体制で第1号「鈴鳴峠の嫁入り」(suzunari)・第2号「影盗り宿の十三号室」(tsukikage_yado)まで投入した後、**ユーザー判断で当面「ステージ+敵+ボス+テキストの物語(依頼文/区間フレーバー/口上/完了文)」のみに縮小**。回想シーン(soulStory)は2クエストからデータ撤去済み(表示機構=effects.jsの回想ビューアは休眠残置、敵defにsoulStoryを足せば復活)、クエスト専用背景の新規制作も停止(既存2ルートのsuzunari_*/tsukikage_*背景は残す)。ボス撃破時のファンファーレ抑止・戻るボタン遅延もsoulStory有無で発動する休眠状態。**iCloudのAI引き継ぎフォルダ(Codex⇔Claude連絡)運用も混乱リスクで廃止**(既存ファイルは履歴として残置、今後読み書きしない)。スキーマ縮小の詳細は`docs/クエスト生成フォーマット.md`冒頭の注記。残り: 2クエストのステータス調整、実機確認。**クエスト取り込みのモデル分担(2026-07-31ユーザー確定)**: パッケージのボスギミックは「創作上の提案」のみ最大2案(主力+戦闘後半の補助。実装方式/数値/トリガー形式は書かれない)。**Sonnet担当=ステージ・背景切り替え(既存背景の使い回し)・敵・文章・通常ボス登録まで**(ボスはギミック未実装の通常戦闘として遊べる状態で納品してよい)。**Fable 5担当=ギミック提案を読み、実装可能なものだけgimmicks構造化データへ翻訳追加**。Sonnetはギミック実装に手を出さずgimmickNotesへの転記だけ行う。**パッケージのiCloud保存先(2026-08-01再変更)**: `iCloudDrive/温泉村物語用/奉行所パッケージ/<日本語クエスト名>/`(例: `狐火相撲の夜`=haratsuzumimaru、`帰らずの湯治宿`=wasureyu_oshira。ユーザーがスマホで判別しやすいよう日本語タイトル名。**questIdはフォルダ名から判断せず各フォルダ内quest.jsonの`questId`を正とする**。フォルダ内のファイル名は従来の英数字固定名のまま。旧・温泉村物語用直下は廃止、旧直下を探さない)。プロジェクト側の正本は従来どおり`assets/quest_raw/<questId>/`(英数字ID)。**敵イラストの制作基準**は`奉行所パッケージ/00_敵イラスト制作基準.md`(雑魚=主役特徴1つ+補助2つまで・4〜5色・シルエット優先、**透過PNGへ接地影を焼き込まない=影はゲーム側のCSS共通表現**(battle.cssの.enemy-card.frameless .enemy-portrait-box::after)、木枠/台座/文字禁止)。新基準のスプライトは敵defに`frameless: true`を付けて配置するだけでよい ⑤**性格の戦闘癖を実装(2026-07-29)、実機確認まだ**: 9性格それぞれに小さな戦闘上の癖(data.jsのPERSONALITY_QUIRKS、無口=沈黙完全無効など)。設計ルール=全て小さなプラス・状況依存・マイナス/回復/MP/ヘイト操作は禁止(ランダム雇用のハズレ排除)。雇用時に性格+癖が判明するモーダル、ステータス画面に癖表示。node vm18テスト全通過。関連する将来案: 英霊システム(戦死者の墓+参拝加護)・発狂の性格別個性化・道中の秘湯イベント・村人の顔(いずれもユーザーへ提案済み未着手)
1. **式神6種のフル実装(2026-07-25)、実機確認まだ**: ユーザー提供のイラスト(1枚絵、6体並び→Pillowで個別切り出し+背景透過済み、assets/icons/shikigami/)+詳細仕様をもとに、紙人形/妖狐/白鶴/狛犬/麒麟/龍神をSHIKIGAMI_DEFS(data.js)として実装。陰陽師のキャラクターレベル(2/4/5/7/9)で解禁が増え、式神召喚時に種類選択ピッカーが出る(battle.jsのrenderShikigamiTypePicker)。各タイプ固有のAI(通常攻撃/連撃/クールダウン制の特技(狐火・回復・結界・スタン・沈黙)/召喚時自動発動効果/紙人形の状況判断型「庇う」/龍神の毎ターン自己回復)をengine.jsのresolveShikigamiAction/tryShikigamiSpecial/applyShikigamiOnSummonに実装。帰還/撃破したタイプはその戦闘中は再召喚不可(owner.__usedShikigamiTypes)。攻撃力は当初陰陽師のmag直接参照で実装したが「魔力が高すぎて攻撃力が過大になる」とユーザー指摘を受け、同レベル帯の他職業基準(紙人形=陰陽師本人のatk、妖狐=狩人-2、白鶴=狩人-3、狛犬=狩人-1、麒麟=侍-1、龍神=槍士と同値)に修正済み(コミット081e976)。召喚エフェクトはComfyUIで専用VFXを生成して一度実装したが「センスない」とユーザー指摘でrevert済み、現状は演出無しでSE(hawk_summon)のみ。node vmでの単体テストは全パターン通過済みだが、実機(スマホ)での見た目・操作感の確認はまだ
2. **侍/槍士/狩人/陰陽師のスキルツリー大幅更新(2026-07-25、コミット30417ba・71c79eb・以降のcounterDamageBonus拡張)、要レビュー**:
   - 侍Lv10右「天衣無縫」(反撃ダメージ+50%、counterDamageBonus)は、counterChance系(燕返し)だけでなく心眼(guardCounterSelf)・見切り/瞬身の順(onEvadeCounterMult)の反撃ダメージにも加算で乗るよう修正済み(ユーザー指摘により対応)。死にスキルではない
   - **明示指定が無く自分の判断で決めた点**: 槍士は蜻蛉切りがLv7左→Lv5左へ移動した際、行き先が示されていなかった剛槍をLv5左→Lv7左へ入れ替えた(要確認)
   - 侍「水月」を敵バフ解除→2ターン強制ターゲット(forceTarget)に、「覚悟」(致命傷でHP1+状態異常全解除)を新規実装、槍士「怒声」(戦闘中1度・HP25%以下限定の全体確定スタン)を新規実装
   - node vmでの単体テストは全て通したが、実機(スマホ)での見た目・操作感の確認はまだ済んでいない
   - 旧・忍者「影分身の術」(2026-07-21分、コミット73a5476)の要レビュー項目(`docs/引き継ぎ_影分身式神.md`)もまだ未回答のまま残っている(式神部分は上記1番で刷新済み)
3. **古城の鍵ブロッカー解消**: 古城は「鍵が無いと実質到達不可」で止まっている。area_map.htmlのメモに「幽霊船=鍵の入手先案」があるので、下記保留中の敵実装と絡めて幽霊船を実装すれば海ルート拡張(岸壁→幽霊船→鍾乳洞の洞窟→荒野)と同時に片付く
4. **スキルの未設定プレースホルダー解消(ほぼ完了)**: 忍者「影分身の術」実装済み。**侍「鬼神化」は2026-07-30にユーザー仕様確定でフル実装済み**(遠征中1回の変身・専用立ち絵・鬼神斬り・ストレス+100デザイン。スキルエディタ差分第2弾、コミット3b7f101)。残りは忍者「禁忌・影分身の術」(desc:後日実装)のみ未設計(再着手時は必ず内容を確認してから)
5. **せりふくん3人版の残り**: 採用310/600本。2026-07-21にID:901〜1050(150本)を追加生成済みで、ID:801〜1050(バッチ9・10、計250本)が判定待ちのままLINES配列に積んである。優先度は下げてよいが、他の合間に少しずつ進める
6. **和風統一の方向性だけ先に決める**: 下記「未決定事項」参照。「完成」の定義に関わるため、作業に迷わないよう早めに決定

### 保留(2026-07-26ユーザー方針: 今は序盤を徹底的に磨き上げる段階のため、新エリア拡張は凍結)
- **山ルート・城ルートへの敵実装**: 渓流/光る竹林/修験道/山/廃城下町/門/古城の7エリアはdata.jsのENEMIESに敵データが0件で、歩くだけで戦闘が一切発生しない。pipeline/areas.jsonに敵の設計テキストは既にある。画像化(ComfyUI、run_nvidia_gpu.batで起動可なことは2026-07-25確認済み。敵画像生成ツールenemy_prompt_generator.htmlは未作成)→data.js投入が主な作業。再開時はこの項を優先リストに戻す

## リリース前に戻すもの
- タイトル画面のタップ後の静止時間: 本来1秒→テスト期間中0.1秒に短縮中(title.jsのrunTitleSequence内sleep(100)、2026-07-28)。リリース前にsleep(1000)へ戻す
- タイトルの「UI調整」トグルボタン+戦闘のサイズ/配置調整パネル一式(2026-08-01): index.htmlのtitleSizeTunerBtn、title.jsのトグル処理、battle.js末尾のinitSizeTuner。リリース前にまとめて削除(?sizeTuner=1のURL起動も同関数)

## 未決定事項(次回セッションで詰める)
- ステータス名・UI文言なども含めて和風に統一するかどうか(「◯層目」以外は概ね西洋ファンタジー風の言い回しが残っている)
- 昼夜は完全に見た目(背景画像)のみの変更(旧・瀕死ロスト判定への影響は瀕死システム廃止とともに消滅、2026-07-26)
