# 温泉村物語

`C:\ダンジョン1\` を複製して作った新プロジェクト。ゲームの仕組み(パーティ制・パーマデス・戦闘システム等)はダンジョン1から引き継ぎつつ、**和製(和風)の方向性に作り変えていく**ことが目的。ダンジョン1自体は元のまま残っており、このプロジェクトはそこから分岐した独立した作業ブランチ。

詳細な過去の作業履歴は docs/開発履歴.md を参照。

## 作業環境の前提(重要)
ユーザーは基本的にパソコンの前におらず、**スマホから遠隔で(リモートコントロールで)作業している**。Claude Codeとのやり取り自体もスマホ経由が基本という前提で動くこと。これを踏まえて:
- 何か確認を求める・操作させる時は「PCの前でこうしてください」ではなく、スマホでそのまま完結する方法を優先する(例: ローカルサーバーを自分で起動して確認、ではなく本番URL`https://onsen-mura-monogatari.pages.dev`をスマホのSafariで開いて確認、が基本ルート)
- 作業用ツール(スキルエディタ・世界地図エディタ等の`*.html`)を新規に作る時も、スマホのブラウザで開いて操作できることを前提にレイアウト・操作方法(タップ/ドラッグ)を設計する
- ローカルサーバー起動やPC上でのファイル確認など「PC前提」の手順が必要な場合は、その旨を明示し、代替のスマホ完結ルートが無いか先に検討する

## CLAUDE.md運用ルール
- このファイルは軽量に保つ。個別の作業ログ・コミット記録は docs/開発履歴.md の末尾に追記し、CLAUDE.mdには書かない。
- CLAUDE.md が 30,000バイトを超えたら古い記録を docs/開発履歴.md へ移す。

## せりふくんシステム
`serifu/`ディレクトリに「せりふくん(軽度ストレス)」というセリフ制作補助システムがある。詳細ルールは`serifu/CLAUDE_serifu.md`参照。
**ユーザーが「せりふくん軽度ストレス生成」のようにこのシステムでのセリフ生成を求めるコメントをしたら、まず現在起動しているモデルを確認する。Fable 5ならそのまま`serifu/CLAUDE_serifu.md`のルールに従って生成に入ってよい。Fable 5でなければ生成せず、「現在Fable 5になっていません」等と伝えて指示を待つこと**(このシステムは品質最優先でFable 5前提のため、無断でSonnet 5等にフォールバックしない)。

## バージョン履歴
- **温泉村物語v1.1**(gitタグ): 8職業和風化、温泉村の舞台美術(町/宿屋/深淵の森の昼夜背景)、瀕死+救出システム、敵妖怪40種フル刷新、難易度再調整(階層スケーリング強化)、温泉(ストレス回復施設)、戦闘UIの視認性改善(表示位置の完全固定化・敵タップ選択・ダメージポップアップ・ログ簡潔化)までを含む。デスクトップに`温泉村物語v1.1を起動.lnk`のショートカットあり
- **温泉村物語v2.3**(gitタグ): v1.1以降の一連のUI安定化(モバイル実機でのfixed配置修正、町画面の2x2ボタンレイアウト、スクロール引っかかり修正)と、ストレス表現の落書きオーバーレイ化+段階デバフ+発狂(行動不能)、宿屋の宿泊制への再編(HP/MP自動回復廃止)、パーティ編成を出発時選択に変更、名簿上限10人までを含む。デスクトップに`温泉村物語v2.3を起動.lnk`のショートカットあり(v1.1のショートカットも残置)

## 次回このプロジェクトを再開する時の手順
1. `C:\温泉村物語\` を開く(このCLAUDE.mdが自動的に読み込まれる)。デスクトップの`温泉村物語 Claude Codeを起動.lnk`をダブルクリックすると、このディレクトリで`claude`が起動した状態のコマンドプロンプトがそのまま開く(コード上は`cmd.exe /k "cd /d C:\温泉村物語 && claude"`)。スマホからも指示を送りたい場合は`温泉村物語0711.lnk`(または既存の`温泉村物語 Claude Code(リモート)を起動.lnk`、中身は同じ)を使う。こちらは`claude --remote-control`付きで起動し、リモート操作(スマホ等)を有効にする
2. ローカルサーバーが必要な場合は `python nocache_server.py`(ポート8127)を`C:\温泉村物語\`で起動、またはデスクトップの`温泉村物語v2.3を起動.lnk`をダブルクリックすると`index.html`を直接ブラウザで開ける(file://起動、サーバー不要。ただしlocalStorageのセーブは同じ起動経路を使い続けないと引き継がれない点に注意)
3. スマホ実機で確認したい場合はCloudflareトンネルを新規に立てる必要がある(トンネルは使い捨てで、前回のURLは失効している)。「スマホで見たい」と伝えれば都度セットアップする。**あるいは本番URL`https://onsen-mura-monogatari.pages.dev`(下記「Cloudflare Pages本番デプロイ」参照)をそのままスマホのSafariで開いてもよい**(常時稼働・トンネル不要)
4. 作業を続ける際は本ファイル(CLAUDE.md)の「バージョン履歴」と各見出しの実装メモを読めば経緯を追える。gitログ(`git log --oneline`)も各コミットが1機能単位でまとまっている

## Cloudflare Pages本番デプロイ(重要な運用ルール)
このプロジェクトは`https://onsen-mura-monogatari.pages.dev`にCloudflare Pagesとして常時公開されており、ユーザーは主にこのURLを実機Safariで開いて動作確認する(トンネルより優先度が高い確認手段)。デプロイは以下のコマンドで行う:
```
npx wrangler pages deploy . --project-name=onsen-mura-monogatari --branch=main --commit-dirty=true
```
**`--branch=main`を必ず明示すること。** このオプションを省略すると、wranglerはgitの現在のローカルブランチ名(このリポジトリでは`master`)を使ってデプロイしてしまい、Cloudflare Pages側のProduction環境は`main`ブランチにひも付いているため、`--branch`未指定だとPreview環境にしか反映されず本番URLが更新されない(実際にこれで「直したのにスマホでは直っていない」という事故が一度発生した)。
デプロイ後は必ず`curl -sL https://onsen-mura-monogatari.pages.dev/index.html`等でファイル内容を取得し、ローカルの該当ファイルと`diff`で完全一致することを確認してから完了報告すること(SPAフォールバックにより存在しないパスでも200が返るため、ステータスコードだけでは正しくデプロイされたか判別できない)。本番エイリアスURL(`onsen-mura-monogatari.pages.dev`)は個別デプロイURL(`https://<hash>.onsen-mura-monogatari.pages.dev`)よりCDNキャッシュの反映が数秒〜数十秒遅れることがあるため、diffが一致しない場合は15〜20秒待って再確認する。

## ダンジョン1から引き継いだ仕組み(ゲームエンジン部分は共通)
- **3人パーティ+控え1人**制、ターン制バトル(2026-07-26の大転換で4人制から変更。出発時に4人選び、4人目=控え。戦闘中は「交代」コマンド=ターン非消費で出たキャラが即行動・パーティ共有CD3ターン。倒れたら控えが無料で自動登場しCDが3にリセット。経験値は参加ターン比=出場ターン数÷戦闘総ターン数で按分)
- MP制(魔法系だけでなく物理必殺技もMP消費。町でのみ回復)
- 疲労度システム(潜り続けると弱体化、町で回復)
- パーマデス(**戦闘不能=100%即ロスト**。旧・瀕死+担ぐ+救出システムは2026-07-26の3人化転換で全廃。即死モードトグルも廃止=標準化、昼夜は完全に見た目のみに。旅団旗の建物効果(5人編成解禁)は無効化済みで再設計待ち)
- 複数敵・範囲攻撃(1〜3体の集団が出現し、範囲技持ちが雑魚の群れに有効)
- 職業ごとの武器/防具(3段階、レベルで解禁、購入すると同職業全員に永久ステータス加算)
- ダンジョン探索は線形進行(グリッド移動なし)。ただしスレスパ風の進路選択あり: フロア前進時に1〜2択の道(林道/獣道/暗い道/静かな道/木漏れ日/光る道/不穏な道/神隠しの道、海岸ステージは別テーマ)から選び、道ごとに戦闘率/財宝率/奇襲/金倍率が変わる。おみくじの結果も抽選に影響(dungeon.jsのPATH_DEFS参照)
- スマホ前提のレイアウト

## 完成に向けた優先順位(2026-07-26更新)
0. **【次回セッションの一発目に必ずやる】戦闘UIの差分更新化**: `docs/引き継ぎ_戦闘UI差分更新化.md` を**着手前に全部読むこと**。iOS実機で戦闘演出全般の出だしフレームが描画されない問題(2026-07-26発見)の根本対策として、renderBattleScreen/renderPartyBarのinnerHTML総入れ替えをやめ、カードDOMを使い回して中身だけ書き換える方式へ移行する。ユーザーと「大規模でも王道の方法でやる」と合意済み。引き継ぎ文書に目的・iOSの法則・コード地図・フェーズ手順・地雷リスト・検証方法まで全部書いてある
0-1. **【積み残し】敵の大技予告に狙われている味方のターゲットマーク+致死警告(2026-07-25、コミット44be95f)、実機確認まだ**: 大技予告(bigAttackPending)が立った瞬間に単体大技の対象を確定し(engine.jsのcommitBigAttackTelegraphTarget、以前は発動時まで対象未定だった仕様を変更)、狙われた味方カードに赤い照準マーク(CSS/SVGのみ、画像素材なし)を表示。タップで敵名/技名の説明、未かばう想定でHPが最大20%以下まで落ち込む可能性が高い場合は左上に⚠️も追加表示しこちらもタップで説明。node vmでの検証は済みだが実機での見た目確認はまだ
1. **式神6種のフル実装(2026-07-25)、実機確認まだ**: ユーザー提供のイラスト(1枚絵、6体並び→Pillowで個別切り出し+背景透過済み、assets/icons/shikigami/)+詳細仕様をもとに、紙人形/妖狐/白鶴/狛犬/麒麟/龍神をSHIKIGAMI_DEFS(data.js)として実装。陰陽師のキャラクターレベル(2/4/5/7/9)で解禁が増え、式神召喚時に種類選択ピッカーが出る(battle.jsのrenderShikigamiTypePicker)。各タイプ固有のAI(通常攻撃/連撃/クールダウン制の特技(狐火・回復・結界・スタン・沈黙)/召喚時自動発動効果/紙人形の状況判断型「庇う」/龍神の毎ターン自己回復)をengine.jsのresolveShikigamiAction/tryShikigamiSpecial/applyShikigamiOnSummonに実装。帰還/撃破したタイプはその戦闘中は再召喚不可(owner.__usedShikigamiTypes)。攻撃力は当初陰陽師のmag直接参照で実装したが「魔力が高すぎて攻撃力が過大になる」とユーザー指摘を受け、同レベル帯の他職業基準(紙人形=陰陽師本人のatk、妖狐=狩人-2、白鶴=狩人-3、狛犬=狩人-1、麒麟=侍-1、龍神=槍士と同値)に修正済み(コミット081e976)。召喚エフェクトはComfyUIで専用VFXを生成して一度実装したが「センスない」とユーザー指摘でrevert済み、現状は演出無しでSE(hawk_summon)のみ。node vmでの単体テストは全パターン通過済みだが、実機(スマホ)での見た目・操作感の確認はまだ
2. **侍/槍士/狩人/陰陽師のスキルツリー大幅更新(2026-07-25、コミット30417ba・71c79eb・以降のcounterDamageBonus拡張)、要レビュー**:
   - 侍Lv10右「天衣無縫」(反撃ダメージ+50%、counterDamageBonus)は、counterChance系(燕返し)だけでなく心眼(guardCounterSelf)・見切り/瞬身の順(onEvadeCounterMult)の反撃ダメージにも加算で乗るよう修正済み(ユーザー指摘により対応)。死にスキルではない
   - **明示指定が無く自分の判断で決めた点**: 槍士は蜻蛉切りがLv7左→Lv5左へ移動した際、行き先が示されていなかった剛槍をLv5左→Lv7左へ入れ替えた(要確認)
   - 侍「水月」を敵バフ解除→2ターン強制ターゲット(forceTarget)に、「覚悟」(致命傷でHP1+状態異常全解除)を新規実装、槍士「怒声」(戦闘中1度・HP25%以下限定の全体確定スタン)を新規実装
   - node vmでの単体テストは全て通したが、実機(スマホ)での見た目・操作感の確認はまだ済んでいない
   - 旧・忍者「影分身の術」(2026-07-21分、コミット73a5476)の要レビュー項目(`docs/引き継ぎ_影分身式神.md`)もまだ未回答のまま残っている(式神部分は上記1番で刷新済み)
3. **山ルート・城ルートへの敵実装(最優先)**: 渓流/光る竹林/修験道/山/廃城下町/門/古城の7エリアはdata.jsのENEMIESに敵データが0件で、歩くだけで戦闘が一切発生しない。pipeline/areas.jsonに敵の設計テキストは既にあるので、画像化→data.js投入が主な作業。**2026-07-21に着手を試みたが、画像生成用のComfyUIサーバーが起動しておらず、敵画像生成ツール(enemy_prompt_generator.html)自体も未作成のため、人力の画像生成ステップが無いと完結しないことが判明した**(2026-07-25にComfyUIサーバー自体は起動できることを確認済み、run_nvidia_gpu.bat)
4. **古城の鍵ブロッカー解消**: 古城は「鍵が無いと実質到達不可」で止まっている。area_map.htmlのメモに「幽霊船=鍵の入手先案」があるので、3と絡めて幽霊船を実装すれば海ルート拡張(岸壁→幽霊船→鍾乳洞の洞窟→荒野)と同時に片付く
5. **スキルの未設定プレースホルダー解消(一部完了)**: 忍者「影分身の術」は実装済み。侍「鬼神化」(desc:後日設定)・忍者「禁忌・影分身の術」(desc:後日実装)はまだ未設計のまま残っている(一度実装を試みたが無断実装だったためユーザー指示でrevert済み、再着手時は必ず内容を確認してから)
6. **せりふくん3人版の残り**: 採用310/600本。2026-07-21にID:901〜1050(150本)を追加生成済みで、ID:801〜1050(バッチ9・10、計250本)が判定待ちのままLINES配列に積んである。優先度は下げてよいが、他の合間に少しずつ進める
7. **和風統一の方向性だけ先に決める**: 下記「未決定事項」参照。「完成」の定義に関わるため、作業に迷わないよう早めに決定

## 未決定事項(次回セッションで詰める)
- ステータス名・UI文言なども含めて和風に統一するかどうか(「◯層目」以外は概ね西洋ファンタジー風の言い回しが残っている)
- 昼夜は完全に見た目(背景画像)のみの変更(旧・瀕死ロスト判定への影響は瀕死システム廃止とともに消滅、2026-07-26)
